services:
  web:
    build: .
    ports:
      - "5858:5858"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - PORT=5858
    command: gunicorn -w 4 -b 0.0.0.0:5858 app:app
    restart: unless-stopped
