version: '3.8'

services:
  web:
    build: .
    ports:
      - "5959:5858"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
      - PORT=5858
    command: gunicorn -w 4 -b 0.0.0.0:5858 app:app
    restart: unless-stopped
