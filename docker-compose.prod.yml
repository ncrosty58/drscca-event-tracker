version: '3.8'

services:
  web:
    build: .
    ports:
      - "5858:5858"
    environment:
      - FLASK_ENV=production
      - PORT=5858
    command: gunicorn -w 4 -b 0.0.0.0:5858 app:app
    restart: unless-stopped
